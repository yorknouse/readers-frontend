---
import { MUSE_SECTIONS } from "../navigation-categories.ts";

export function getStaticPaths() {
  return MUSE_SECTIONS.map((section) => ({params: {section : section.section}}))
};

const { section} = Astro.params;
---
<h1>Muse - {section}</h1>